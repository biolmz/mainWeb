=========================================
 9P 文件系统演示 (virtio-9p)
=========================================
本目录由浏览器的 HTTP 服务器通过 v86 的
virtio-9p 设备暴露给客户机 (TinyCore Linux)。

客户机已在启动时自动挂载到 /mnt/9p:
    ls -la /mnt/9p

说明:
- 这里的文件来自宿主(浏览器)侧, 对客户机只读。
- 想在客户机里用 gcc 编译 C 程序, 请切换到
  "TinyCore Linux  gcc 工具链" 模式 (已内置 gcc/make)。
- 也可以在本 9P 模式里直接读取这些源文件, 例如:
    cat /mnt/9p/host_hello.c
    cat /mnt/9p/process_list.c
- v86 还提供 create_file API, 可把客户机产生的
  文件写回宿主(浏览器)侧, 实现双向共享。
